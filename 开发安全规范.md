# 项目开发安全规范

> 本文件是硬性约定，所有参与本项目开发的人（含 AI 助手）必须遵守。

## 数据库数据保护（最高优先级）

**绝对禁止：无条件全表删除。** 例如以下命令任何时候都不允许执行：

```sql
DELETE FROM users;                     -- 禁止！
DELETE FROM creation_tasks;            -- 禁止！
TRUNCATE TABLE users;                  -- 禁止！
DROP TABLE users;                      -- 禁止！
```

### 必须遵守的规则

1. **所有 DELETE 必须带 WHERE 条件**，且条件必须限定到具体数据范围。
2. **测试脚本统一使用 199 号段手机号**（`199xxxxxxxxxx`），
   清理测试数据只允许按号段删除：
   ```sql
   DELETE FROM users WHERE phone LIKE '199%';
   ```
3. **测试数据清理只允许通过 `clean_test_data.py` 执行**
   （该脚本内置号段限制 + 二次确认，见项目根目录）。
4. **删除数据前先备份**：
   ```bash
   mysqldump -u root -p010819 ai_content_generator > backup_$(date +%Y%m%d).sql
   ```
5. **真实用户数据（非 199 号段）视为不可删除**，任何清理操作都不得波及。

### 已注册的真实账号（勿动）

| 手机号 | 说明 |
|--------|------|
| （用户自行注册的账号） | 由用户手动注册，绝不清理 |

## 环境与密钥

- `.env` 含真实 API Key，禁止提交到 git（已在 .gitignore）
- 新增配置必须走 `.env`，禁止硬编码

## 代码规范

- async 异步写法，禁止同步阻塞代码
- 类型注解完整（typing）
- Pydantic v2 做请求/响应校验
- 统一异常：自定义 BizException + 全局异常处理器，不裸 raise
- 日志用内置 logger，禁止 print
- 配置全部从 .env 读取
