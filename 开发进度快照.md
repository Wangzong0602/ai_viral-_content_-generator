# 开发进度快照（2026-08-03）

> 重开会话后：先读 `AGENTS.md` 了解项目全貌，再读本文件了解进度。
> 继续开发时从「下一步建议」开始，向用户确认后开工。

## 已完成功能（全部实测通过）

| 模块 | 状态 | 说明 |
|------|------|------|
| 用户系统 | ✅ | 注册/登录/登出/JWT+Redis 会话/资料管理 |
| 多智能体创作 | ✅ | LangGraph：选题→逻辑分析→创作→润色→排版→质检→事实核查 |
| 历史记录 | ✅ | 列表/详情/复用/删除 + 收藏/搜索/平台筛选 |
| AI 配图 | ✅ | wan2.7-image-pro + 本地存储 + 记录落库（关联 task） |
| 多平台适配 | ✅ | 首屏多选 → 主版本完成后自动生成各平台版本（字数规范校验） |
| 爆文逆向分析 | ✅ | 输入链接/内容 → 7 维爆文要素拆解 |
| 事实真实性保障 | ✅ | 创作前联网搜索注入 + 事实核查节点 + 前端风险警告条 |
| 批量生成 | ✅ | Celery threads pool，上传关键词列表批量生成，进度轮询 |
| 数据看板 | ✅ | ECharts 图表（趋势/平台/质量）+ 概览卡片 |
| 内容模板库 | ✅ | 9 个种子模板（每平台 3 种爆款结构），创作时可选 |
| 后台管理 | ✅ | 仅管理员：全局统计/用户管理/内容管理（封禁/设管理员/删除） |
| 导出 | ✅ | Markdown（含配图引用）/ HTML（图片内嵌）/ 纯文本 |
| 会员系统（2026-08-03 新增） | ✅ | 三档套餐（免费/专业199/企业999）+ 下单/虚拟支付/续费/订单 |
| 支付骨架（2026-08-03 新增） | ✅ | 虚拟支付可用；微信/支付宝渠道骨架（.env 开关，默认关闭） |
| 后台套餐管理（2026-08-03 新增） | ✅ | 套餐增删改/上架下架/权益 JSON 配置 |
| 后台订单管理（2026-08-03 新增） | ✅ | 订单列表（状态/关键词筛选）+ 用户列表会员列 + 统计金额 |
| 权益配额控制（2026-08-03 新增） | ✅ | 按会员等级限制每日生成/分析/配图/批量次数（Redis 日计数） |
| 会员到期提醒（2026-08-03 新增） | ✅ | 临期（≤3天）提醒条/工作台弹窗提示 + 已过期提醒 + 管理端手动续期 |
| 单元/API 测试（2026-08-04 新增） | ✅ | 70 个用例全过，覆盖率 63%（pytest + SQLite 内存库 + fakeredis + AI mock） |
| 多内容形态（2026-08-04 新增） | ✅ | 视频脚本/直播文案/电商带货：Tab 化分离（形态创作不依赖平台），形态驱动 prompt |
| 更多平台（2026-08-04 新增） | ✅ | 新增 B站/快手/视频号：适配 prompt/字数规范/排版/选题/逻辑分析/种子模板（18 个） |
| 独立 SEO 智能体（2026-08-04 新增） | ✅ | 关键词提取/密度优化/话题标签/标题优化/Meta 描述（图文形态，脚本类跳过） |

## 管理员账号

- **19900000001 / admin123456**（系统管理员）
- 用户自己的真实账号（18779704543）也被设为管理员了（之前测试误操作，可改回普通用户）

## 当前代码状态

- 独立 SEO 智能体已完成，尚未提交 git（需提交）
- 备份分支：
  - `backup/visual-refactor`（之前的一次视觉重构，被回滚）
  - `backup/ui-optimize-7b9a447`（ui-ux-pro-max 优化，被回滚——布局出问题）
- 未跟踪文件：frontend/scripts/verify_*.png/cjs（测试截图，可忽略或删除）

## 用户的重要偏好（务必遵守）

1. **数据安全**：禁止无条件删表！测试账号只用 199 号段，清理走 `clean_test_data.py`
2. **前端改动要谨慎**：用户对"布局出问题"非常敏感，已两次回滚视觉重构。
   改前端样式后必须浏览器实测所有页面，确认无错位
3. **代码注释要详细中文**：用户是初学者，要求注释解释"为什么"
4. **代码规范**：async、类型注解、Pydantic v2、BizException、logger、配置走 .env
5. **完成功能后自主测试**：用户期望我自行验证（后端接口 + 前端 Playwright）

## 下一步建议（按优先级）

1. **提交并推送本次 SEO 智能体**
2. **剩余需求功能**：Word(.docx) 导出（半天工作量）/ 订单退款 / 用户详情增强
3. **P3 最后一项**：开放 API（B 端计费，改动最大）
4. **或测试扩展**：AI 服务 mock 测试补全、GitHub Actions CI
5. **或前端视觉打磨**（用户之前不满意基础版视觉，但两次重构都回滚了——
   若要做，需保守渐进式改造 + 充分实测，参考 backup/ui-optimize-7b9a447 的 bug 修复点）

## 多平台说明（P3 扩展）

- 支持平台：小红书/公众号/知乎/B站/快手/视频号（6 个）
- 各平台字数规范（适配校验）：小红书 300-700、公众号 1200-3000、知乎 1500-3000、
  B站 800-2000、快手 300-800、视频号 1200-3000
- 种子模板：18 个（每平台 3 种），增量补种（按 平台+名称 判断缺失才插入，
  老库自动补上新平台模板）
- 前端平台列表 4 个页面已同步（工作台/历史/批量/后台）

## SEO 智能体说明

- 位置：润色 → SEO → 排版（条件边：仅图文形态走 SEO，脚本/直播/带货跳过）
- 产出：优化后文案（关键词密度微调+文末话题标签）+ 关键词列表 + 话题标签 + Meta 描述 + 优化标题
- 平台差异：小红书/B站/快手加话题标签，知乎/公众号/视频号不加（优化标题与描述）
- 前端：结果区展示 SEO 报告（优化标题/关键词/标签/搜索描述）
- 注意：max_tokens 需 8192（正文 2000+ 字时 4096 会被截断导致 JSON 解析失败）

## 测试体系说明

- 运行：`python -m pytest tests/ -v`（在项目根目录）
- 覆盖率：`python -m pytest tests/ --cov=app --cov-report=term-missing`（当前 63%）
- 三个隔离：SQLite 内存库（不动真实 MySQL）/ fakeredis（不动真实 Redis）/ AI 全 mock（不花钱）
- 已覆盖：security、user_service、membership_service(96%)、quota_service(97%)、batch_service、
  template、dashboard、auth/user/membership/quota/admin API 全流程、内容形态校验（75 个用例）
- 未覆盖（低优先级）：content_service/adapt/fact_check/image 等 AI 服务（mock 成本高，价值低）

## 多内容形态说明

- 内容形态：article 图文（默认）/ video_script 视频脚本 / live_script 直播文案 / ecommerce 电商带货
- **Tab 化分离**：工作台输入区顶部 4 个 Tab——图文 Tab 保留平台/模板/多平台选项；
  形态 Tab 只留关键词输入（无平台选择器），形态创作与平台解耦
- 后端：非 article 形态创作时不注入平台信息（选题/逻辑分析/创作 prompt 均按形态驱动），
  保证视频脚本/直播文案/电商带货输出纯正
- 数据库：creation_tasks.content_type 列（已 ALTER 添加）
- 历史记录/结果区显示形态标签
- 已实测：电商带货输出 2322 字纯电商结构（痛点/卖点），无平台污染；Playwright 全流程通过

## 遗留的未提交代码改动

- 多内容形态（schemas/models/agents 四节点/content_service/api/前端工作台与历史/测试 5 例）
- 修复：润色环节丢失脚本结构标记 bug（脚本类形态专用润色 prompt）
- 测试体系已提交（0e9c335）、会员到期提醒已提交（701ec1b）、权益配额已提交（c316c5a）、会员系统已提交（63c9002）
